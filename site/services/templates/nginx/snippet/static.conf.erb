location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|pdf|woff|woff2|ttf|eot|webp)$ {
    expires 7d;

    add_header Cache-Control public;
    add_header Access-Control-Allow-Origin *;
    add_header Vary Accept-Encoding;

    access_log off;
}
