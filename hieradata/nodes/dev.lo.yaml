---
classes:
    - role::work

projects:
    cem:
        - { name: www, tpl: 'docker', port: 8900, ws: [ '/build' ] }
        - { name: s3, tpl: 'proxy', port: 8900, ws: [ '/ws' ] }
        - { name: s3-cli, tpl: 'proxy', port: 8900, ws: [ '/ws' ] }
        - { name: conv, tpl: 'proxy', port: 8900 }
        - { name: styleguide, tpl: 'docker', port: 8903, ws: ['/ws', '/storybook-server-channel'] }
        - { name: emails, tpl: 'docker', port: 8905 }
        - { name: holidays, tpl: 'docker', port: 8906 }
    cryengine:
        - { name: www, php: php8.1, tpl: 'symfony' }
        - { name: forum, php: php8.1, tpl: 'phpbb' }
        - { name: cuas, php: php8.1, tpl: 'symfony' }
        - { name: mage, php: php8.1, tpl: 'magento' }
        - { name: gs, php: php8.1, tpl: 'symfony' }
        - { name: docs, php: php8.3, tpl: 'symfony' }
        - { name: styleguide, php: ph8.1, tpl: 'default' }
        - { name: conv, tpl: 'proxy', port: 9400 }
        - { name: s3, tpl: 'proxy', port: 8080, ws: [ '/ws' ] }
        - { name: s3-cli, tpl: 'proxy', port: 8081, ws: [ '/ws' ] }
    oxcom:
        - { name: www, php: php7.4, tpl: 'symfony' }
        - { name: pg, php: php7.4, tpl: 'symfony' }
        - { name: auto, php: php7.4, tpl: 'symfony' }
    arthustle:
        - { name: www, tpl: 'docker', port: 3000, ws: [ '/ws', '/storybook-server-channel' ] }
        - { name: api, tpl: 'proxy', port: 3000, ws: [ '/ws', '/storybook-server-channel' ] }
        - { name: s3, tpl: 'proxy', port: 3000 }
        - { name: s3-cli, tpl: 'proxy', port: 3000, ws: [ '/ws', '/storybook-server-channel' ] }
        - { name: conv, tpl: 'proxy', port: 3000 }
        - { name: storybook, tpl: 'docker', port: 6006, ws: [ '/ws', '/storybook-server-channel' ] }
    sushi:
        - { name: www, tpl: 'docker', port: 8940, ws: [ '/ws' ] }
        - { name: s3, tpl: 'proxy', port: 8940 }
        - { name: s3-cli, tpl: 'proxy', port: 8940, ws: [ '/ws' ] }
        - { name: conv, tpl: 'proxy', port: 8940 }
    space42:
        - {name: www, tpl: 'docker', port: 8950, ws: [ '/ws' ]}
        - { name: s3, tpl: 'proxy', port: 8940 }
        - { name: s3-cli, tpl: 'proxy', port: 8940, ws: [ '/ws' ] }
        - { name: conv, tpl: 'proxy', port: 8940 }

php:
    composer: true
    versions: [ php7.3, php7.4, php8.1, php8.3, php8.4 ]
    packages: [
        opcache, gd, intl, mbstring, curl,
        mysql, readline, soap, sqlite3, tidy, xml, zip,
        codecoverage, codesniffer, igbinary, imagick,
        redis, xdebug, ssh2, bcmath, apcu, amqp
    ]

tools: [
    terminator, firefox, chromium-codecs-ffmpeg-extra, chromium-browser,
    imagemagick, webp, libwebp-dev, php-json
]

db:
    mariadb:
        root_password: root
        remove_default_accounts: true
        override_options:
            client:
              ssl: false
            mysqld:
                bind-address: '0.0.0.0'
                skip_ssl: 1
                ssl-disable: true
                wait_timeout: 28800
                interactive_timeout: 28800
                max_allowed_packet: 1024M
                # will cache frequently read data
                innodb_buffer_pool_size: 4G
                # larger buffer reduces write I/O to Transaction Logs
                innodb_log_buffer_size: 256M
                # larger log file reduces checkpointing and write I/O
                innodb_log_file_size: 1G
                # service write operations to .ibd files. 
                innodb_write_io_threads: 8
                # flushes the InnoDB Log Buffer to the Transaction Logs (ib_logfile0, ib_logfile1) once per second
                innodb_flush_log_at_trx_commit: 0

docker:
    # Allow users to execute docker commands
    users: [ 'vagrant' ]
