---
classes:
    - role::work

projects:
    cryengine:
        - {name: www, php: php7.3}
        - {name: forum, php: php7.1}
        - {name: cuas, php: php7.3}
        - {name: mage, php: php7.1}
        - {name: gs, php: php7.4}


php:
    composer: true
    versions: [php7.1, php7.3, php7.4]

#  packages: [
#      opcache, gd, bcmath, curl, intl, json, mbstring,
#      mysql, readline, soap, sqlite3, tidy, xml, zip,
#      codecoverage, codesniffer, igbinary, geoip, imagick,
#      memcache, memcached, redis, xdebug, ssh2
#  ]


tools: [imagemagick]

docker:
    # Allow users to execute docker commands
    users: ['vagrant']

#bind9:
#  dns: ['8.8.8.8', '8.8.4.4']
